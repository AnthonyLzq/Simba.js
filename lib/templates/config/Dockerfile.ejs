FROM node:22-alpine
<% if (managerName === 'pnpm') { %>
RUN corepack enable
<% } %>
WORKDIR /app

COPY . ./

RUN <%- managerName === 'yarn' ? 'yarn' : managerName === 'npm' ? 'npm' : 'pnpm' %> i

EXPOSE 1996

CMD [ "<%- managerName %>", "start" ]