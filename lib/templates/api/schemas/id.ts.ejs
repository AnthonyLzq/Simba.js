import z from 'zod'

const id = <% if (dbIsSQL) { %>z.preprocess(val => Number(val), z.number())<% } else { %>z.string()<% } %>

type Id = z.infer<typeof id>

const idSchema = z.object({ id })

type IdSchema = z.infer<typeof idSchema>

export { id, Id, idSchema, IdSchema }
